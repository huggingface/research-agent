schema_version: 1
name: discover
version: 0.2.0
description: "Discover skills, MCP servers, A2A agent cards, and federated registries with Agent Resource Discovery."
commands:
  discover:
    description: Discover resources with Agent Resource Discovery
    input_hint: "[--federation none|auto|referrals] [--follow-referrals] <query>"
    handler: ./discover.py:discover
